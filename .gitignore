# macOS
.DS_Store
.AppleDouble
.LSOverride

# Editors / IDE
.vscode/
.idea/
*.swp
*.swo
*~
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Env / secrets
.env
.env.*
!.env.example

# Worktrees
.worktrees/
.claude/worktrees/

# Logs
*.log

# Swift / SPM
.build/
.swiftpm/
DerivedData/
*.xcodeproj/
# Package.resolved はライブラリでは commit する。アプリ単体なら ignore でも可

# 言語別追加（必要に応じて scaffold 時に append）
# Rust:        target/, Cargo.lock (libの場合は除外しない)
# Node:        node_modules/, dist/, .next/, *.tsbuildinfo
# Python:      __pycache__/, *.pyc, .venv/, dist/, build/
# Go:          bin/, vendor/
